# Supabase Configuration (Required for dynamic project data)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Note: The portfolio will work without Supabase - it uses placeholder data as fallback
# To enable dynamic content management via /admin, set up Supabase and add these values
